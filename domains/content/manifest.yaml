# Content / writing domain — skills for Telegram posts, articles, technical tutorials.
# Requires no env. Global in ~/.claude/.

name: content
description: Content — Telegram posts, articles, technical tutorials.

requires_env: []

targets:
  rules:  ${CLAUDE_HOME}/rules
  skills: ${CLAUDE_HOME}/skills
  agents: ${CLAUDE_HOME}/agents
